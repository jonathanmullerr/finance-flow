name: Quality checks on PR 🧐

on:
  pull_request:
    paths-ignore:
      - "**.md"

jobs:
  rspec:
    name: Rubocop & RSpec
    runs-on: ubuntu-latest
    steps:
      - name: Install bundler
        run: |
          gem install bundler

      - name: Bundler install
        run: |
          bundle

      - name: Run Rubocop
        run: |
          rubocop

      - name: Run Rspec
        run: |
          rspec
